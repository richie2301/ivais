import React from "react";
import { Typography } from "antd";

const { Title } = Typography

const MissionMapView: React.FC = () => {
    return (
      <div>
        <Title>Mission Map View</Title>
      </div>
    );
  }
  
  export default MissionMapView;